# Lab 3 - AI Use and Reflection

**LLM/agent used:** OpenAI Codex coding agent (GPT-5-family model exposed by the Codex workspace)

The table records real prompts used while planning and implementing Lab 3. It is intentionally limited to prompts that materially changed the Lab 3 plan, implementation, or workflow. The final personal reflection will be written after implementation and verification; it will not be reconstructed from memory.

## Selected key prompts

| # | Prompt | Purpose | What the AI suggested | Student decision/correction | Verification |
|---|---|---|---|---|---|
| 1 | “Now time for Lab 3; read it carefully and deeply understand it, then make `PLAN_LAB3.md` in `docs/lab-03`.” | Start Lab 3 planning from the labsheet and repository context. | Read the Lab 3 PDF and repository baseline, then proposed a contract-first plan with scope, roles, migration, APIs, UI, tests, evidence, and staged Issues. | Accepted the contract-first structure and reviewed the proposed authentication, staff, and admin scope against the labsheet before implementation. | The resulting plan was inspected in `docs/lab-03/PLAN_LAB3.md`; the PDF headings, exclusions, and evidence requirements were cross-checked. |
| 2 | “Let start do Issue 33 by preparing the contract documents on the current Lab 3 branch.” | Begin the documentation-only contract Issue without jumping to product code. | Checked the current branch/baseline, ran the required-file red-phase check, and drafted `specification.md`, `api-spec.md`, `ui-spec.md`, and the living evidence documents. | Accepted the Issue #33 scope and kept Git/PR/Project mutations out of this turn. | The expected failing guard was observed before the six contract files existed. The new documents were then inspected for FR/BR/AC/Test traceability, exclusions, and secret-safe wording. |
| 3 | “Finished merging the Lab 3 contract; start Issue #34 authentication foundation from the latest `lab3-staging`, using the approved branch and failing-first workflow.” | Start the next implementation Issue only after the contract merge and begin with planned authentication tests. | Created `feature/14-Lab3AuthFoundation` from the merged staging baseline, ran the planned missing-module red phase, then implemented validation, session-security primitives, additive auth schema/migration, seed safeguards, and initial auth API routes. | Accepted the bounded Issue #34 foundation scope, preserved Lab 2 routes for later authenticated ownership migration, and did not commit, push, or open a PR. | The red-phase failure was recorded in `tests.md`; focused Lab 3 tests (22/22), full server regression (62/62), server build, disposable migration, two seed runs, seeded login, and credential-preservation checks passed. Client and later feature Issues remain deferred. |
| 4 | “Read the inline comments on PR #44, determine which findings belong to Issue #34, fix them without expanding later Issue scope, rerun the affected tests, and prepare the same branch for review.” | Review the actual PR feedback and correct only the migration/seed and Issue #34 evidence gaps. | Fetched both inline comments, changed credential backfill to cover every legacy Requester, added a non-fixture Ticket/Attachment ownership regression, added wrong-CSRF and idle/absolute-expiry API regressions, and refreshed the evidence counts. | Accepted both findings as in-scope corrections; kept later authenticated Requester, staff, admin, UI, and E2E work deferred. No merge or approval was claimed. | Focused Lab 3 tests passed 7 files/25 tests; full server regression passed 19 files/65 tests; server build, disposable migration, and two repeated seed runs passed. |
| 5 | “The previous Issue #34 is merged; start Issue #35 from the latest `lab3-staging` using the approved failing-first workflow for authenticated Requester continuity.” | Begin the authenticated Requester regression only after the authentication foundation merge. | Created `feature/15-Lab3RequesterAuthRegression`, wrote API/UI/E2E red tests, then implemented session-derived ownership, protected reference access, route guards, the authenticated shell, Public Comments, resolution indication, and the additive migration/seed correction. | Accepted the bounded Requester scope, removed selector/header trust from the product flow while preserving the Lab 2 Ticket/Attachment routes, and kept commit, push, PR, review, and merge actions unauthorized. | Red failures were observed before implementation; focused API, migration/seed, UI, build, Playwright, and adapted inherited Lab 2 regression checks were rerun after implementation. The complete authenticated Requester workflow E2E remains a later release gate. |
| 6 | “Fix the four actionable review comments on PR #45, keep the changes within Issue #35, run the planned failing tests first, and verify the fixes.” | Address the partner’s logout, session-expiry, resolution-status, and Change Password route findings without expanding the Issue. | Added failed-logout preservation/retry, a shared `SESSION_REQUIRED` redirect handler, explicit `CLOSED`/`CANCELLED` resolution rules, and direct `/change-password` guarding with regressions. | Accepted all four findings as in-scope corrections; kept full authenticated Requester workflow E2E and release evidence deferred. Commit/push/PR actions remain separately authorized. | The red phase recorded 3 client and 1 server failures. The focused client suite passed 6 tests, the resolution unit test passed, full server/client suites passed 21/69 and 8/32, and both builds passed. |
| 7 | “Read the current inline comments in PR #45, reproduce each finding, fix the requested race and route-guard issues with failing-first tests, and update the evidence.” | Address the new stale session-restore race and missing Staff/Admin protected routes without expanding Issue #35. | Added an auth-generation guard that ignores stale `currentUser()` results after Login/logout/session expiry, suppresses stale initial-restore `401` notifications, and protects `/staff/tickets`, `/staff/tickets/:ticketNumber`, and `/admin/users`. | Accepted both comments as real in-scope corrections; kept the safe-user timestamp mismatch, clean pre-migration database proof, and full release evidence as separate release-gate work. No commit, push, PR, or approval was claimed in this turn. | The red phase recorded 4 client failures. The focused UI suite passed 10 tests, the complete client suite passed 8 files/36 tests, and the client production build passed. |
| 8 | “The previous Issue #35 is merged; start Issue #36 from the latest `lab3-staging` on `feature/16-Lab3StaffQueue`, implement the approved IT Staff Ticket Queue with failing-first tests, and leave Git mutations unauthorized.” | Begin the Staff Queue implementation with the approved API/UI contract, safe role boundaries, queue data, URL-controlled query state, and focused verification. | Added the additive Ticket status/IT Priority/owner migration, protected staff queue and assignee APIs, strict query parsing and exact response serialization, deterministic idempotent queue seed fixtures, API client validators, responsive table/card UI, loading/empty/no-results/forbidden/failure states, and focused API/UI tests. | Accepted the bounded Issue #36 scope; kept Staff Ticket Detail operations, comments/notes, Administrator management, Playwright release flows, and merge/push/PR actions for their approved later Issues or explicit authorization. Did not add credentials or use the development database for test cleanup. | The planned red phase observed the missing-route and missing-screen failures. The guarded test migration targeted `toktickit_lab2_test`; focused server queue tests passed 5/5, focused client queue tests passed 4/4, server build passed, and full client tests passed 9 files/40 tests. A full server attempt was not claimed as passing because this disposable database retained hashes from an earlier generated seed password and the new process-only password was rejected/rate-limited; no credential was written or recorded. |
| 9 | “Read the friend’s P1 comment on PR #46, fix the missing Staff Queue Open Detail behavior within Issue #36, run failing-first regression tests, and prepare the branch for review.” | Address the required Open action without taking on the later Staff Ticket Operations mutations. | Added the protected `GET /api/staff/tickets/:ticketNumber` detail contract, exact safe role/not-found behavior, a read-only staff detail screen, URL navigation, and API/UI regressions. | Accepted the P1 as an in-scope completion of FR-09/AC-11’s Open action; kept assignment, status, comments, notes, Attachment operations, and merge/Project actions deferred or separately authorized. | The red phase recorded the missing 404 detail response and absent client fetch function. The fixed server queue/detail suite passed 6/6 and the fixed client queue/detail suite passed 5/5. |
| 10 | “Let’s start Issue #37 by implementing the approved IT Staff Ticket operations from the latest `lab3-staging`, using failing-first tests and leaving commit, push, PR, and merge unauthorized.” | Begin the staff assignment, IT Priority/status, comment/note, and Attachment-continuity work without changing the Administrator Issue or release evidence scope. | Added transactional staff assignment/priority/status APIs, the complete transition matrix and confirmation rules, staff-readable Attachment continuity, role-aware Public Comments, append-only Internal Notes with an additive migration, API client methods, and an operational Staff Ticket Detail screen. | Accepted the bounded Issue #37 scope and preserved Requester-only Attachment upload/removal and existing Lab 2 routes. Kept the real test-database migration requirement visible instead of marking the Internal Note test as passed before the migration was applied. | Planned API tests were observed failing before implementation. After implementation, focused staff API tests passed 4/4, status unit tests passed 2/2, focused Staff Detail UI tests passed 3/3, the Staff Queue regression passed 5/5, both builds passed, and a follow-up audit added coverage that reopening clears old resolution metadata. The notes API remains pending only because the local disposable database still needs the new migration. |

| 11 | “Finished merging Issue #37; start Issue #38 from the latest `lab3-staging`, implement Administrator User Management with failing-first tests, and leave commit, push, PR, review, and merge actions unauthorized.” | Start the Administrator User Management Issue with the approved API/UI contract and a safe test-first workflow. | Added Administrator-only user list/search/filter, one-role create/edit/activation, initial-password reset, duplicate/ownership/last-admin protections, session revocation, a responsive table/card screen, and focused API/UI tests. | Accepted the bounded Issue #38 scope; preserved the existing Requester/Staff responsibilities, linked newly managed Requester accounts to their Requester identity, and did not authorize Git mutations. | The API red phase first failed on missing routes and the UI red phase failed on the missing component. The guarded API suite passed 5/5, the focused UI suite passed 3/3, the server/client builds passed, and the complete client suite passed 11 files/47 tests. Full server regression, migration/seed repeat, E2E, and release evidence remain later gates. |

| 12 | "Fix the three review comments on Issue #38: add direct sole-Administrator coverage, verify mobile User Management cards, and synchronize the tests.md matrix statuses." | Address the partner's requested test-coverage and traceability corrections without expanding the Administrator User Management product scope. | Added an isolated sole-active-Administrator state-preservation regression, mobile card/Edit-action coverage with shared responsive CSS assertions, and synchronized API-10/UI-06 counts and statuses. | Accepted all three comments because they improve direct AC-19 coverage, responsive evidence, and document accuracy; no product scope was expanded. | The guarded Administrator API suite passed 6/6; the User Management plus shared style tests passed 8/8; the complete client suite and both builds were rerun. The full server/release gates remain separate environment/migration work. |

The inherited server and client regression suites were subsequently adapted to the authenticated session contract and passed. The earlier table wording that described this adaptation as pending was a red/green-phase snapshot; the current passing counts are recorded in `tests.md`. The remaining full authenticated Requester workflow E2E is intentionally tracked as a later release gate.

## Verification and human decisions

- The assistant's proposed session, CSRF, password, migration, role, queue, status-transition, and User Management decisions remain explicitly marked as contract decisions in `specification.md`; implementation must not silently expand them.
- The student retained authority over branch creation, commits, pushes, PRs, reviews, Project status, and merges. Issue #35 implementation work is limited to this local feature branch; commit, push, PR, review, Project, and merge mutations remain separately authorized actions.
- Remaining planned test IDs and paths in `tests.md` are not claimed as passing. They will be replaced or confirmed with actual paths and complete output as each Issue is implemented.
- The real failing-first output is preserved in `tests.md`; no successful result is inferred merely from document creation.

## Reflection

To be completed by the student after Lab 3 implementation and final evidence review. The reflection must explain what was learned, what AI suggestions were accepted or rejected, how outputs were verified, and why human judgment was necessary.
